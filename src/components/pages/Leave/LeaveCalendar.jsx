import * as React from "react";
import { makeStyles } from "@mui/styles";
import "./LeaveCalender.css";

const style = makeStyles({
  accordionDetails: {
    padding: "0 !important",
  },
});

export default function LeaveCalendar() {
  const classes = style();

 return <div>Leave Calender</div>
}
